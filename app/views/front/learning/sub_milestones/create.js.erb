

$("#milestone_<%= j (@sub_milestone.milestone_id.to_s) %>_sub_milestones").append(
  "<%= j (render partial: 'front/learning/sub_milestones/sub_milestone', locals: { sub_milestone: @sub_milestone } ) %>"
  );

// Remove the form fields of the sub_milestone_form
$(".new_sub_milestone_form").find("#milestone_name").val("");
$(".new_sub_milestone_form").find("#milestone_days").val("");

// Update the days field of the parent milestone
$("#milestone_<%= j (@sub_milestone.milestone_id.to_s) %>_days").html(
    "Planned for <%= j (@milestone.sub_milestones_days.to_s) %> days"
    );

